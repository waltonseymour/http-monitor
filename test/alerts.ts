/// <reference path='../typings/index.d.ts' />
/// <reference path='../node.d.ts' />
import { assert } from 'chai';

describe('Alerts', () => {
  it('shoud something', () =>{
    assert(1 === 1);
  });
});
